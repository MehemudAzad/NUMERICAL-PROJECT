# Core stack for the local analytic suite (Tiers 1 & 2) and the test suite.
# Tiers 1 & 2 are CPU-only NumPy — they run fine on a laptop with no GPU.
#
# Install (this Mac's Homebrew python@3.14 is broken on macOS 26, so use uv):
#   uv venv --python 3.12 .venv && source .venv/bin/activate
#   uv pip install -r requirements.txt
# or on a normal machine:
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

numpy==2.5.2
scipy==1.18.1
matplotlib==3.11.1
pandas==3.0.5
pytest==9.1.1
torch==2.14.0          # needed by third_party/dpm_solver_pytorch.py (arm C), CPU is fine for Tiers 1-2

# Notebook execution
jupyterlab
ipykernel

# --- Tier 3 only (M9/M11; run on a Kaggle T4, not on a laptop) ---
# notebooks/09_tier3_cifar10.ipynb and notebooks/11_samples_fid.ipynb pip-install
# these themselves on Kaggle, so they stay out of the pinned laptop environment --
# tests/test_tier3.py deliberately needs none of them (it builds the checkpoint's
# schedule from synthetic betas and a fake model_fn).
# diffusers
# accelerate
#
# clean-fid: M11 revives a *small-scale* FID anchor (guide Part 5's Gate G2
# allowed cutting it entirely; M11 instead uses it as a validation check against
# the paper's Table 6 ranking, not as the primary Tier-3 read-out -- L2-to-
# reference stays that). Installed by notebook 11 itself on Kaggle.
# clean-fid
